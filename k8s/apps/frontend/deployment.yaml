apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: taskhub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: localhost:32000/taskhub-frontend:latest
        ports:
        - containerPort: 80
